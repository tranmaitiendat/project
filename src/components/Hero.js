import React from 'react';
import './Hero.css';

const Hero = () => {
    return (
        <section className="hero">
            <div className="hero-content">
                <h1>Your Trusted Property Insights</h1>
                <p>Empowering property decisions with expert analytics</p>
                <button className="cta-button">Free Discovery Call</button>
            </div>
        </section>
    );
};

export default Hero;
